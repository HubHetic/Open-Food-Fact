import numpy as np
import pandas as pd


def pointer_database():
    vect1 = np.zeros(10)
    vect2 = np.ones(10)
    df = pd.DataFrame({'col1': vect1, 'col2': vect2})
    return df


def find_code(vecteur):
    return 2


def restart_vector(model_cnn, database_image):
    vect1 = np.zeros(10)
    vect2 = np.ones(10)
    df = pd.DataFrame({'col1': vect1, 'col2': vect2})
    return df


def save_base_vector(pointeur_bd_vecteur):
    pass
